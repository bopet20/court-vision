import { useEffect } from 'react'

const usePlayer = () => {
  useEffect(() => {
    const getPlayer = async () => {
      const response = await fetch()
    }
  })

}

export default usePlayer
